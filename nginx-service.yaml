apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  type: NodePort
  selector:
    app: multi-container-example
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080  # This will expose nginx on port 30080 on all nodes 