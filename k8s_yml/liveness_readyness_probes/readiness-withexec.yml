---
apiVersion: v1
kind: Pod
metadata:
  name: readiness-exec
  labels:
   type: readiness-exec
   app: test
spec:
  containers:
    - name: test-readiness-exec
      image: busybox  
      args:
        - sh
        - -c
        - touch /tmp/healthy; sleep 30; rm /tmp/healthy; sleep 600
      readinessProbe:
        exec:
          command:
            - cat 
            - /tmp/healthy
        initialDelaySeconds: 10
        periodSeconds: 5     
#   - name: test-liveness-exec1
#     image: busybox1  
#     args:
#       - sh
#       - -c
#       - touch /tmp/healthy1; sleep 30; rm /tmp/healthy; sleep 600
#     readiness:
#       exec:
#         command:
#           - cat 
#           - /tmp/healthy1
#       initialDelaySeconds: 10
#       periodSeconds: 5 