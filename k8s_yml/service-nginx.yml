---
apiVersion: v1
kind: Service
metadata:
  name: apache-service
spec:
  selector:
    Env: qat
    type: apache-server
  ports:
    - port: 80 
      targetPort: 80
      protocol: TCP 
  type: ClusterIP