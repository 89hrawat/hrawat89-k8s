apiVersion: v1
kind: Service
metadata:
  name: redis-service
spec:
  type: NodePort
  selector:
    app: multi-container-example
  ports:
    - port: 6379
      targetPort: 6379
      nodePort: 30379  # Redis will be exposed on port 30379 